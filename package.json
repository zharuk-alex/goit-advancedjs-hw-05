{
  "name": "typescript_vsc_settings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc --watch & web-dev-server --node-resolve --open --watch",
    "build": "tsc && mkdir -p build_dist && mv ./dist/*.js build_dist/ && mv ./dist/*.js.map build_dist/ && cp index.html build_dist/ && mv build_dist dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.4.5"
  },
  "dependencies": {
    "prettier": "^3.3.2"
  }
}
